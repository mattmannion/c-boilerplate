{
  "nodemonConfig": {
    "ext": "c",
    "signal": "SIGTERM",
    "exec": "make && make run || exit 1",
    "effects": {
      "start": "clear"
    },
    "events": {
      "start": "clear"
    }
  },
  "scripts": {
    "d": "make clean && nodemon",
    "r": "make clean && make",
    "s": "make clean && make && make run"
  }
}
